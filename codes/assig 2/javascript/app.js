var userEmail = "Arham mazhar";
var userPwd = "12345";

var email = prompt("enter your email")
var pwd = prompt("enter your password")

var isEmailCorrect = email === userEmail;
var isPwdCorrect = pwd === userPwd;

if (email== userEmail) {
    if (pwd === userPwd) {
        alert("Login success");     
    } else {
        alert("credential not found")
    }
}

con

